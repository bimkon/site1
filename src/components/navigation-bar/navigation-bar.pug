include ../../components/button/button.pug
include ../../components/submenu/submenu.pug
mixin navigation-bar({ values= [{link, text, links}], isLogged, subMenu})
    case isLogged
        when 'logged'
            .navigation-bar
                .navigation-bar__body
                    .navigation-bar__logo-toxin
                        +logo-toxin()
                    .navigation-bar__right-column
                            nav.navigation-bar__navigation
                                ul.navigation-bar__top-menu
                                    each value in values
                                        - const { link, text, links, isActive} = value
                                        case isActive
                                            when 'true'
                                                li.navigation-bar__links
                                                    a.navigation-bar__link.navigation-bar__link_active(href=link)=text
                                                    if Array.isArray(links) && links.length != 0
                                                        +submenu({links})
                                            default
                                                li.navigation-bar__links
                                                    a.navigation-bar__link(href=link)=text
                                                    if Array.isArray(links) && links.length != 0
                                                        +submenu({links})
                            .navigation-bar__user
                                img.navigation-bar__arrow(src="rectangle.svg", alt="вертикальная линия")
                                .navigation-bar__text='Юлий Цезарь'
        default
                .navigation-bar
                    .navigation-bar__body
                        .navigation-bar__logo-toxin
                            +logo-toxin()
                        .navigation-bar__right-column
                            nav.navigation-bar__navigation
                                .navigation-bar__burger
                                ul.navigation-bar__top-menu
                                    each value in values
                                        - const { link, text, links, isActive} = value
                                        case isActive
                                            when 'true'
                                                li.navigation-bar__links
                                                    a.navigation-bar__link.navigation-bar__link_active(href=link)=text
                                                    if Array.isArray(links) && links.length != 0
                                                        +submenu({links})
                                            default
                                                li.navigation-bar__links
                                                    a.navigation-bar__link(href=link)=text
                                                    if Array.isArray(links) && links.length != 0
                                                        +submenu({links})
                                .navigation-bar__buttons
                                    .navigation-bar__login-button
                                        +button({type: 'button', text: 'Войти', color: 'white', size: 'short'})
                                    .navigation-bar__password-button
                                        +button({type: 'button', href:'/mock-address/change-me',text:'Зарегистрироваться', size: 'longer', })
